# 正确的HASH模块架构设计

## 问题分析

### 原架构的问题
原设计采用了16个独立pipeline并行处理不同block，但这是错误的，因为：
1. **Hash算法是迭代的**: MD5/SHA256/SHA1都是迭代hash算法
2. **状态累积**: 每个512bit block的处理结果会更新hash状态，影响下一个block
3. **顺序依赖**: Block之间是顺序依赖关系，不能并行处理

### 正确的理解
- **一个大数据流**: 需要顺序处理整个数据流
- **Block级流水线**: 多个block可以在pipeline的不同stage同时处理
- **状态传递**: 每个block处理完成后，hash状态传递给下一个block

## 正确的架构设计

### 1. 深度Pipeline架构

```
┌─────────────────────────────────────────────────────────┐
│              AXI-Stream Input Interface                  │
│              (256bit, 接收数据流)                          │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│           Block Formatter & Buffer Manager               │
│           - 组装512bit blocks                            │
│           - 双缓冲机制（ping-pong buffer）                │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│           Block Queue (FIFO)                             │
│           - 缓存多个blocks                               │
│           - 支持流水线处理                               │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│        Deep Pipeline Hash Core                          │
│                                                          │
│  Stage 1: Block接收和分解                                │
│    ↓                                                     │
│  Stage 2-N: 消息扩展/轮计算（深度pipeline）              │
│    ↓                                                     │
│  Stage N+1: Hash状态更新                                 │
│                                                          │
│  多个blocks可以在不同stage同时处理                        │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│              AXI-Stream Output Interface                 │
│              (256bit, 输出最终hash结果)                   │
└─────────────────────────────────────────────────────────┘
```

### 2. 关键设计要点

#### 2.1 Block级流水线
- **多个block同时处理**: Block N在stage M处理时，Block N+1可以在stage M-1处理
- **状态传递**: 每个block处理完成后，hash状态传递给下一个block
- **流水线深度**: 等于算法处理一个block所需的cycle数

#### 2.2 深度Pipeline优化
- **MD5**: 64轮可以分成多个pipeline stage
- **SHA256**: 64轮 + 消息扩展可以深度pipeline
- **SHA1**: 80轮可以深度pipeline

#### 2.3 吞吐率提升
- **理论最大吞吐率**: 当pipeline填满后，每个cycle可以完成一个block的处理
- **实际吞吐率**: 考虑pipeline填充和排空的开销

## 性能分析

### Pipeline深度设计

#### MD5 Pipeline设计
```
Stage 1: Block接收和分解 (1 cycle)
Stage 2-17: 数据准备 (16 cycles, 可优化)
Stage 18-81: 64轮主循环 (64 cycles, 深度pipeline)
Stage 82: Hash状态更新 (1 cycle)

总深度: 82 stages
优化后: 45-50 stages (通过并行和优化)
```

#### SHA256 Pipeline设计
```
Stage 1-2: Block接收 (2 cycles)
Stage 3: 消息扩展初始化 (1 cycle)
Stage 4-19: 消息扩展 (16 cycles, pipeline)
Stage 20-83: 64轮主循环 (64 cycles, 深度pipeline)
Stage 84: Hash状态更新 (1 cycle)

总深度: 84 stages
优化后: 55-60 stages
```

#### SHA1 Pipeline设计
```
Stage 1-2: Block接收 (2 cycles)
Stage 3: 消息扩展初始化 (1 cycle)
Stage 4-25: 消息扩展 (22 cycles, pipeline)
Stage 26-105: 80轮主循环 (80 cycles, 深度pipeline)
Stage 106: Hash状态更新 (1 cycle)

总深度: 106 stages
优化后: 60-65 stages
```

### 吞吐率计算

#### 理论最大吞吐率
当pipeline填满后，每个cycle可以完成一个block的处理：
- **MD5**: 64 bytes / cycle = 64 GB/s @ 1GHz（理论）
- **SHA256**: 64 bytes / cycle = 64 GB/s @ 1GHz（理论）
- **SHA1**: 64 bytes / cycle = 64 GB/s @ 1GHz（理论）

#### 实际吞吐率
考虑pipeline填充和排空：
- **填充时间**: 等于pipeline深度（cycles）
- **排空时间**: 等于pipeline深度（cycles）
- **有效处理时间**: 总时间 - 填充时间 - 排空时间

对于大数据流（N个blocks >> pipeline深度）：
- **实际吞吐率** ≈ **理论最大吞吐率** × **效率因子**
- **效率因子**: 约90-95%（考虑填充和排空）

#### 达到15GB/s的要求
- **需要**: 15 bytes/cycle @ 1GHz
- **每个block**: 64 bytes
- **需要**: 64 / 15 ≈ 4.3 cycles/block
- **实际**: MD5需要45 cycles, SHA256需要55 cycles, SHA1需要60 cycles

**结论**: 单个深度pipeline无法达到15GB/s，需要：
1. **进一步优化cycle数**（不现实，算法限制）
2. **多个独立hash实例**（如果真的是多个独立数据流）
3. **重新评估需求**（15GB/s可能不现实，或需要ASIC优化）

## 正确的实现方案

### 方案1: 深度Pipeline + Block流水线（推荐）

#### 架构
- 单个hash核心，但内部深度pipeline
- 支持多个block流水线处理
- Block队列缓存

#### 吞吐率
- **MD5**: ~1.4 GB/s（45 cycles/block）
- **SHA256**: ~1.2 GB/s（55 cycles/block）
- **SHA1**: ~1.1 GB/s（60 cycles/block）

#### 优点
- 架构正确，符合hash算法特性
- 实现相对简单
- 资源使用合理

#### 缺点
- 无法达到15GB/s（单个数据流限制）

### 方案2: 多个独立Hash实例（如果真的是多个数据流）

如果需求是处理多个独立的数据流，那么：
- 每个数据流使用独立的hash实例
- 16个实例可以并行处理16个数据流
- 每个实例内部深度pipeline

#### 吞吐率
- **16个实例**: 16 × 1.2 GB/s = ~19 GB/s（SHA256）

#### 适用场景
- 多个独立数据流
- 每个数据流独立hash

### 方案3: 超深度Pipeline优化（ASIC级别）

#### 架构
- 将算法进一步并行化
- 使用ASIC级别的优化技术
- 可能需要重新设计算法实现

#### 吞吐率
- 可能达到15GB/s，但需要大量资源

## 资源评估（深度Pipeline架构）

### 单Pipeline深度Pipeline版本

| 模块 | MD5 (LUT) | SHA256 (LUT) | SHA1 (LUT) |
|------|-----------|--------------|------------|
| Hash核心（深度pipeline） | ~8,000 | ~12,000 | ~11,000 |
| Block队列（FIFO） | ~2,000 | ~2,000 | ~2,000 |
| AXI接口 | ~2,000 | ~2,000 | ~2,000 |
| Block格式化 | ~1,300 | ~1,300 | ~1,300 |
| **总计** | **~13,300** | **~17,300** | **~16,300** |

### 16个独立实例版本（如果多个数据流）

| 模块 | MD5 (LUT) | SHA256 (LUT) | SHA1 (LUT) |
|------|-----------|--------------|------------|
| 16×Hash核心 | ~128,000 | ~192,000 | ~176,000 |
| 16×Block队列 | ~32,000 | ~32,000 | ~32,000 |
| 共享接口 | ~2,000 | ~2,000 | ~2,000 |
| **总计** | **~162,000** | **~226,000** | **~210,000** |

## 建议

### 1. 明确需求
- **单个数据流**: 使用深度pipeline架构，吞吐率~1-1.5 GB/s
- **多个独立数据流**: 使用多个独立实例，16个实例可达~19 GB/s

### 2. 重新评估15GB/s目标
- **单个数据流**: 15GB/s不现实（算法限制）
- **多个数据流**: 15GB/s可以实现（需要16个实例）

### 3. 实现建议
- 先实现深度pipeline版本（正确架构）
- 如果确实需要15GB/s，考虑多个独立实例
- 或者重新评估需求是否合理
