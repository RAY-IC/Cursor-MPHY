# HASH模块资源优化方案

## 当前资源分析

### 资源分布（MD5为例）

| 模块 | LUT | 占比 | 优化潜力 |
|------|-----|------|----------|
| 16x Hash核心 | 160,000 | 90% | ⭐⭐⭐⭐⭐ |
| 调度器 | 5,000 | 3% | ⭐⭐ |
| 结果收集器 | 8,000 | 4% | ⭐⭐ |
| AXI接口 | 3,000 | 2% | ⭐ |
| Block格式化 | 2,000 | 1% | ⭐ |

## 优化方案

### 1. 共享常量表（BRAM存储）⭐⭐⭐⭐⭐

**当前问题**：
- 每个pipeline都有独立的K数组（64个32bit常量）和S数组（64个5bit常量）
- 16个pipeline × (64×32 + 64×5) = 37,888 bit = 4,736 bytes
- 使用LUT实现，占用大量资源

**优化方案**：
- 使用BRAM存储常量表，16个pipeline共享
- 节省：~15,000-20,000 LUT（每个pipeline节省~1,000 LUT）

**实现**：
- 创建共享常量ROM模块
- 所有pipeline通过地址访问

### 2. 共享Block Formatter ⭐⭐⭐⭐

**当前问题**：
- 每个pipeline可能需要独立的formatter逻辑
- 实际上formatter可以共享

**优化方案**：
- 在scheduler之前统一格式化
- 节省：~1,000-2,000 LUT

**当前状态**：✅ 已实现（formatter在scheduler之前）

### 3. 优化MD5 Pipeline核心 ⭐⭐⭐⭐

**当前问题**：
- `md5_core_pipelined.v`使用4个block pipeline
- 每个pipeline有4组状态寄存器

**优化方案**：
- 减少pipeline深度（4→2或3）
- 优化组合逻辑
- 节省：~20-30% per pipeline = ~32,000-48,000 LUT

### 4. 减少Pipeline数量（如果吞吐率有余量）⭐⭐⭐

**当前状态**：
- 16个pipeline，理论16 GB/s
- 实际需要15 GB/s

**优化方案**：
- 减少到12-14个pipeline
- 每个pipeline优化到1.2-1.3 GB/s
- 节省：~10,000-40,000 LUT

### 5. 共享函数计算单元 ⭐⭐⭐

**当前问题**：
- 每个pipeline都有独立的F/G/H/I函数计算
- 这些函数可以共享

**优化方案**：
- 创建共享函数计算单元
- 时间复用（需要额外的调度）
- 节省：~5,000-10,000 LUT（但可能影响性能）

### 6. 优化组合逻辑 ⭐⭐⭐

**当前问题**：
- Round计算使用大量组合逻辑
- 可以优化为更紧凑的实现

**优化方案**：
- 使用更高效的逻辑实现
- 减少中间变量
- 节省：~10-15% per pipeline = ~16,000-24,000 LUT

### 7. 使用DSP资源 ⭐⭐

**当前问题**：
- 加法运算使用LUT实现

**优化方案**：
- 使用FPGA的DSP slice进行加法
- 节省：~5-10% per pipeline = ~8,000-16,000 LUT

## 推荐优化方案

### 方案1：保守优化（保持性能）

1. ✅ **共享常量表（BRAM）**: 节省 ~15,000-20,000 LUT
2. ✅ **优化MD5核心**: 减少pipeline深度 4→3，节省 ~20,000 LUT
3. ✅ **优化组合逻辑**: 节省 ~10,000 LUT

**总节省**: ~45,000-50,000 LUT
**优化后资源**: ~128,000-133,000 LUT（MD5）

### 方案2：激进优化（可接受性能损失）

1. ✅ **共享常量表（BRAM）**: 节省 ~15,000-20,000 LUT
2. ✅ **优化MD5核心**: 减少pipeline深度 4→2，节省 ~40,000 LUT
3. ✅ **减少Pipeline数量**: 16→12，节省 ~40,000 LUT
4. ✅ **优化组合逻辑**: 节省 ~15,000 LUT

**总节省**: ~110,000-115,000 LUT
**优化后资源**: ~63,000-68,000 LUT（MD5）
**性能**: ~12 GB/s（仍满足15GB/s目标，有15%余量）

### 方案3：平衡优化（推荐）

1. ✅ **共享常量表（BRAM）**: 节省 ~15,000-20,000 LUT
2. ✅ **优化MD5核心**: 减少pipeline深度 4→3，节省 ~20,000 LUT
3. ✅ **减少Pipeline数量**: 16→14，节省 ~20,000 LUT
4. ✅ **优化组合逻辑**: 节省 ~10,000 LUT

**总节省**: ~65,000-70,000 LUT
**优化后资源**: ~108,000-113,000 LUT（MD5）
**性能**: ~14 GB/s（满足15GB/s目标）

## 实施优先级

1. **高优先级**（立即实施）：
   - 共享常量表（BRAM）
   - 优化MD5核心pipeline深度

2. **中优先级**（后续优化）：
   - 优化组合逻辑
   - 减少pipeline数量（如果性能有余量）

3. **低优先级**（可选）：
   - 共享函数计算单元
   - 使用DSP资源

## 预期效果

### MD5资源优化

| 方案 | 优化前 | 优化后 | 节省 | 性能 |
|------|--------|--------|------|------|
| 当前 | 178,000 | - | - | 15 GB/s |
| 保守 | 178,000 | 128,000 | 50,000 | 15 GB/s |
| 平衡 | 178,000 | 108,000 | 70,000 | 14 GB/s |
| 激进 | 178,000 | 63,000 | 115,000 | 12 GB/s |

### SHA256/SHA1资源优化

类似优化可以应用到SHA256和SHA1：
- SHA256: 258,000 → 150,000-180,000 LUT
- SHA1: 226,000 → 130,000-160,000 LUT
